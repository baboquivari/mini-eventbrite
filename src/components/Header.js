import React from 'react';
import PropTypes from 'prop-types';

const Header = (props) => {
  return (
    <div className="header">
      wot-zon-jon?
    </div>
  );
};

Header.PropTypes = {};

export default Header;