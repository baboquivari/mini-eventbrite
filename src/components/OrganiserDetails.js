import React from 'react';
import PropTypes from 'prop-types';

const OrganiserDetails = (props) => {
  return (
    <div>
      Order Summary
    </div>
  );
};

OrganiserDetails.PropTypes = {};

export default OrganiserDetails;